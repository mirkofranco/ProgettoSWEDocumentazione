before_install:
  - sudo apt-get update
  - sudo apt-get install hunspell
  - sudo apt-get install myspell-en-us
  - sudo apt-get install myspell-it
  - sudo apt-get install python2.7
  - sudo apt-get install apt-get install python-dev libxml2-dev libxslt1-dev antiword unrtf poppler-utils pstotext tesseract-ocr flac ffmpeg lame libmad0 libsox-fmt-mp3 sox libjpeg-dev swig
  - pip install textract 
jobs:
  include:
    - stage: test
      name: "orthography"
      script: 
       - bash scripts/grammar.sh
notifications:
  slack: zerosevengroup:awH2p9uSzEPY9plMSyKBtAHF
